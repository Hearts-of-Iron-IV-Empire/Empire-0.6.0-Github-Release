political_actions = {
	ZAP_russian_capitulation = {

		icon = generic_decisison

		days_re_enable = 60 

		cost = 25

		ai_will_do = {
			factor = 100
		}

		visible = {
			has_war_with = SOV
			original_tag = ZAP
			NOT = { ZAP = { has_country_flag = ZAP_defeated_by_russia }}
		}

		available = {
			SOV = { surrender_progress > 0.15 }
		}	

		complete_effect = {
		SOV = {
			country_event = russia.25
		}	
	  }
	}
	ZAP_polish_capitulation = {

		icon = generic_decisison

		days_re_enable = 60 

		cost = 25

		ai_will_do = {
			factor = 100
		}

		visible = {
			has_war_with = POL
			original_tag = ZAP
			NOT = { ZAP = { has_country_flag = ZAP_defeated_by_poland }}
		}

		available = {
			POL = { surrender_progress > 0.15 }
		}	

		complete_effect = {
		POL = {
			country_event = poland.52
		}	
	  }
	}
}